    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/settings_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="8dip"
        android:paddingLeft="8dip"
        android:paddingRight="8dip"
        android:paddingTop="6dip"
        android:orientation="vertical"
        >

        <TextView
            android:id="@+id/setting_user_styles"
            style="@style/TextAppearance.Aard2.ListTitle"
            android:text="@string/setting_user_styles"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginRight="14dip"
            android:textSize="20sp"/>

        <!-- The list of styles. When empty there is no placeholder text - the
             Add button below already makes the empty state clear. -->
        <LinearLayout
            android:id="@+id/setting_user_styles_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="vertical"
            >

        </LinearLayout>

        <!-- Add-a-style action on its own button below the list, rather than a
             "+" fused to the section title. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/setting_btn_add_user_style"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="6dp"
            android:text="@string/setting_user_styles_add" />

    </LinearLayout>
