<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/article_collection_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- AppBarLayout's own statusBarForeground (color set programmatically
         in Application.applyStatusBarAppearance() - status bar icons are
         drawn by the system and follow the DEVICE's own dark mode, not
         this app's own separate light/dark preference, so the backdrop
         color has to be chosen the same way, not via a static ?attr/
         reference into this activity's own theme) paints the status bar
         area; AppBarLayout draws it pinned to the true top of the window
         (it negates its own current scroll offset before drawing), so it
         stays put even once the Toolbar below has fully hidden.
         AppBarLayout's own android:fitsSystemWindows="true" is what makes
         it actually capture the dispatched WindowInsets into its internal
         lastInsets field at all - confirmed by decompiling it: it installs
         its own OnApplyWindowInsetsListener on itself in its constructor,
         but that listener only stores the insets (as opposed to
         discarding them) when AppBarLayout.getFitsSystemWindows() is
         true; without it, getTopInset() silently returns 0 forever. The
         Toolbar ALSO needs its own android:fitsSystemWindows="true" (see
         below) - that's what getTotalScrollRange() checks (for a first
         child) to subtract getTopInset() from its collapsible range, so
         the bar can only ever collapse down to the status bar floor, never
         past it. Both flags are required together; either alone is a
         silent no-op. All of this is real, built-in AppBarLayout behavior,
         not something we're approximating (see git history for an
         earlier, hand-rolled overlay-scrim + OnOffsetChangedListener /
         translationY version that had visible timing glitches a manual
         per-frame listener couldn't avoid). -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true">

        <android.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?android:attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:fitsSystemWindows="true"
            android:titleTextColor="?attr/colorOnPrimary"
            android:subtitleTextColor="?attr/colorOnPrimary"
            android:theme="@style/ThemeOverlay.Aard2.Toolbar"
            android:popupTheme="@style/ThemeOverlay.Aard2.ToolbarPopup"
            app:layout_scrollFlags="scroll|enterAlways" />

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.viewpager.widget.PagerTitleStrip
            android:id="@+id/pager_title_strip"
            android:background="?attr/colorSurfaceVariant"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:paddingTop="2dp"
            android:paddingBottom="2dp" />

    </androidx.viewpager.widget.ViewPager>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
